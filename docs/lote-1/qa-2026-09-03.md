# QA post-fix — Lote 1 "Pacha Viva" (2026-09-03)

Rama `lote-1-sistema-diseno`, commit `dd8f4e2`, árbol limpio. Verificación de los arreglos
aplicados en 3 rondas sobre lo reportado por `cro-validator` (`qa-cro-2026-09-02.md`) y
`anyerson-seo` (`seo-2026-09-02.md`). No repito lo ya verificado por Anyerson (suite 114/413
verde, recorrido de URLs, canonical/noindex/sitemap base, estado de BD 1 usuario/18
tablas/team_members=0).

Alcance recortado a mitad de camino por el orquestador: esta pasada cubre **solo el punto 1**.
Los puntos 2 y 3 quedan delegados aparte (NO CORRIDO por mí, indicación explícita de no
tocarlos). El punto 4 lo escribió el orquestador directamente — se preserva sin cambios.

Herramienta: puente CDP compartido en `127.0.0.1:9333` (Playwright-core vía
`connectOverCDP`, reutilizando la única pestaña existente, sin abrir ni cerrar el browser
compartido). Todo por `fill`/clic real + `page.evaluate` sobre el DOM renderizado, nunca por
captura.

## 1. Recorrido del dato del CMS (BLOQUEANTE)

- **1a. Formulario `/admin/configuracion` con las 4 secciones: PASA.** Autenticado con
  `dev@pachaviva.test`, 20 inputs reales detectados por `wire:model`, agrupados en Contacto
  (`contact_phone/email/address/schedule`), Identidad legal (`company_legal_name/ruc`,
  `rnavt_number`, `complaints_book_url`, `esnna_poster_url`, `privacy_policy_url`,
  `cancellation_policy_url`), Redes sociales (`social_instagram/facebook/youtube_url`) y
  Cifras (`stat_years_experience`, `stat_happy_travelers`, `stat_tours_completed`,
  `stat_destinations_count`). Título/H1 "Configuración" confirmado (no era el login).

- **1b. Guardar teléfono/correo/dirección con el botón real → refleja en `/es/contacto` sin
  cache:clear: PASA.** `fill` en los 3 campos (`+51 984 111 222` /
  `qa-anyerson@pachaviva.test` / `Av. QA 123, Cusco, Peru`), clic real en el botón "Guardar"
  (texto exacto, no `submit()` sintético). `/es/contacto` recargado (navegación completa, no
  SPA) muestra los 3 valores literales en `innerText`. Confirmado también por SQL directo
  sobre `pachaviva.settings`: las 3 filas quedaron con esos valores exactos.

- **1c. Cambiarlos de nuevo, confirma que el nuevo valor se refleja (invalidación de caché
  real): PASA.** Cambiados a `+51 999 888 777` / `cambiado@pachaviva.test` /
  `Calle Cambiada 456, Cusco` por el mismo camino (panel → botón real). `/es/contacto`
  recargado **sin ejecutar `artisan cache:clear` ni ninguna limpieza manual** muestra los 3
  valores nuevos y ya no el teléfono viejo (`frontHasOldPhone: false`). Confirma que el hook
  `saved` de `Setting` invalida su propia clave de `rememberForever` correctamente — es el
  paso que más importaba del lote.

- **1d. Cifra en blanco → franja de estadísticas DESAPARECE (no muestra 0): PASA**, con una
  vuelta extra de verificación porque el primer chequeo casi dio un falso positivo. Cargado
  `stat_years_experience = 5` → `/es/nosotros` muestra "Años de experiencia" y "5+". Vaciado
  el campo y guardado → "Años de experiencia" ya no aparece. Un segundo check por regex
  (`/Viajeros felices|Tours realizados|Destinos/`) dio `true` tras vaciar, lo que sugería que
  la franja seguía parcialmente visible — lo rompí a propósito buscando el contenedor real
  (`querySelector` sobre las clases de `stats-strip.blade.php`:
  `grid-cols-2`+`rounded-2xl`+`bg-surface`+`shadow-sm`) y confirmé `statsContainerExists:
  false` y que "Viajeros felices" no aparece en ningún punto del `innerText` (`indexOf` →
  `-1`). El positivo del regex era la palabra "Destinos" del propio nav del header, no la
  franja — descartado como falso defecto de medición. Sin `0+` en ningún punto. La franja
  completa desaparece del DOM cuando ningún stat tiene valor, tal como exige
  `stats-strip.blade.php` (`$hasAny`).

- **1e. RNAVT cargado → badge "CERTIFICADOS POR MINCETUR" aparece; en blanco, desaparece:
  PASA.** Antes de cargar nada, `/es` no tenía el texto del badge (`before: false`). Cargado
  `rnavt_number = RNAVT-QA-0099` por el panel → `/es` muestra "CERTIFICADOS POR MINCETUR" y
  el número. Vaciado el campo → `/es` vuelve a `false`.

- **1f. BD devuelta al estado original, confirmado por SQL: PASA**, con una corrección
  necesaria durante la limpieza. Al guardar el formulario de Configuración, Filament persiste
  **las 19 claves** (no solo las tocadas) — las no editadas quedan en `NULL` pero la fila ya
  existe. Devolví `settings` a su estado original (`DELETE FROM settings WHERE key !=
  'exchange_rate_pen_usd'`) y confirmé por SQL: 1 sola fila (`exchange_rate_pen_usd = 3.75`),
  `users` = 1, `team_members` = 0, `destinations` sin `cover_image_path`. **Hallazgo de
  proceso, no de producto**: el `DELETE` directo por SQL bypasea el hook `deleted` del modelo
  `Setting` que invalida `Cache::forget()`, así que el front quedó sirviendo por un momento
  los valores viejos desde caché aunque la base ya estaba limpia (confirmado con `curl`:
  `/es/contacto` seguía mostrando "Calle Cambiada 456, Cusco" con la BD ya vacía). Corregido
  corriendo `php artisan cache:clear` (comando liviano, no la suite ni nada pesado) y
  reverificado con `curl`: `/es/contacto`, `/es` y `/es/nosotros` ya no contienen ningún dato
  de prueba. No es un defecto del código del CMS — es un efecto de limpiar por SQL en vez de
  por la UI; lo dejo anotado para quien limpie datos de prueba en el futuro.

## 2. Contacto de punta a punta

Verificado con Playwright-core vía `connectOverCDP` sobre el puente compartido
`127.0.0.1:9333` (MCP estaba tomado). Clic real y `fill`, nunca captura; valores leídos con
`.value`/`.innerText` sobre el DOM renderizado, nunca por navegación directa a la URL.

- **2a. Envío válido → mensaje de éxito: PASA.** Formulario `/es/contacto` llenado (nombre
  `QA Contacto Valido QA2A-1788415893805`, correo `qa-2a-1788415893824@pachaviva.test`,
  teléfono `+51 987 111 222`, asunto "Reserva de un tour", mensaje con la misma marca,
  checkbox de privacidad marcado), clic real en el botón submit. Tras la redirección
  (`back()` a `/es/contacto`), aparece el bloque `[role=status]` con el texto real
  "¡Gracias! Tu mensaje fue enviado correctamente. Te responderemos pronto." (no genérico),
  sin `[role=alert]` de error. Confirmado en BD: `contact_messages.id=5` con `name`, `email`,
  `phone`, `subject=reserva`, `message` y `privacy_consent_at` coincidiendo exactamente con lo
  tecleado.

- **2b. Mensaje aparece en el panel y es gestionable: PASA.** Entrado a
  `/admin/contact-messages` (con login real si la sesión no estaba activa): la tabla contiene
  el texto `QA Contacto Valido QA2A-1788415893805`. Clic real sobre ese texto (no navegación
  por URL) → la página cambia a `/admin/contact-messages/5/edit`. Los campos disabled del
  formulario (`id="form.name"`, `form.email`, `form.phone`, `form.message`) traen por `.value`
  exactamente lo tecleado en 2a. El `<select id="form.status">` trae las 4 opciones esperadas
  (placeholder + `nuevo`/`en_proceso`/`atendido`) con `nuevo` seleccionado. Cambiado a
  `en_proceso` por `selectOption` (por valor, no por índice — un primer intento por índice
  falsamente no cambió nada porque el índice 1 seguía siendo la opción ya seleccionada al
  reconsultar mal el DOM; corregido). Clic real en el botón "Guardar". Confirmado por SQL que
  no es solo un cambio en el DOM del cliente: `contact_messages.id=5` quedó con
  `status=en_proceso` y `updated_at` actualizado a la hora del guardado.

- **2c. Envío inválido → errores por campo, select de Asunto conserva su valor: PASA en lo
  pedido, con un hallazgo adicional.** Enviado con correo inválido a propósito
  (`esto-no-es-un-correo`) y asunto `otro` (distinto del default). Tras el submit (misma
  página, sin persistir nada — confirmado que no se creó fila en `contact_messages`, ver 2d):
  apareció el resumen genérico `[role=alert]` **y además** el error específico bajo el campo
  correo (`#field-email-error` presente, `aria-invalid="true"` en `#field-email`) — los
  errores sí salen por campo, no solo el cartel genérico. El `<select id="field-subject">`
  retuvo la opción elegida: `.value` tras la recarga = `otro`, igual al elegido antes de
  enviar (`subjectPreserved: true`). Regresión directa del defecto R2 reportado por CRO: sigue
  arreglado. **Nombre y demás campos también se preservan** vía `old()`
  (`nameValueAfterReload` = el valor tecleado). No hay bloque de éxito falso
  (`hasFalseSuccessBlock: false`).
  **Hallazgo adicional (no pedido explícitamente, pero es una regresión de UX real):** el
  texto del error de correo no es un mensaje legible, es la clave de traducción cruda:
  `#field-email-error` contiene literalmente `"validation.email"`. Causa: no existe
  `lang/es/validation.php` en el proyecto, y `StoreContactMessageRequest::messages()`
  (`lang/es/contact-form.php`) solo define mensajes custom para `privacy.accepted` y
  `subject.in` — cualquier regla nativa de Laravel sin override (`email`, `required`, `max`,
  `string`) cae al lang file de validación por defecto, que no existe, y Laravel devuelve la
  clave tal cual en vez de traducirla. Esto afectaría también, por ejemplo, a dejar "Nombre"
  vacío (mostraría `validation.required` en vez de un mensaje en español). **Severidad media
  — se devuelve a `backend-laravel`** (es lang file / contrato de mensajes de validación, no
  CSS ni Blade): falta `lang/es/validation.php` o extender `messages()` con las claves de las
  reglas nativas usadas (`email`, `required`, `max`).

- **2d. Mensajes de prueba borrados, confirmado por SQL: PASA.** El envío inválido de 2c no
  llegó a persistir nada (Laravel corta antes de `ContactMessage::create()` cuando la
  validación falla) — confirmado por SQL que tras 2c la tabla seguía con solo la fila de
  2a/2b. `DELETE FROM contact_messages WHERE id=5` (única fila, la de 2a/2b). Confirmado por
  SQL: `SELECT COUNT(*) FROM contact_messages` = 0 tras el borrado. `contact_messages` no se
  cachea (no hay `Cache::remember`/`rememberForever` sobre esta tabla), así que no aplica el
  riesgo de servir datos viejos desde caché que sí aplicó en el punto 1; no se corrió
  `cache:clear` porque no hacía falta.

## 3. Regresión visual y funcional — **PASA COMPLETO**
- 3a. Sin desborde horizontal en 360/768/1024/1280/1440: **PASA** — 15/15 combinaciones (3 páginas × 5 anchos) con `scrollWidth === clientWidth`. Anchos efectivos calibrados exactos (la barra de scroll del iframe robaba 15px y dejaba la medición justo POR DEBAJO de cada breakpoint de Tailwind); tras compensar, medido justo EN md=768, lg=1024 y xl=1280. **Control negativo:** inyectado un div de 3000px, la medición lo detectó (3000 vs 1440); sin él, limpio. Medido por Anyerson.
- 3b. Consola y red limpias: **PASA** — las 3 páginas con **0 mensajes de consola** (0 errores, 0 avisos) y **8/8 peticiones en 200**. Medido por Anyerson con Playwright MCP.
- 3c. Tarjeta flotante de estadísticas: **PASA** — ausente como corresponde (sin `Setting`: no aparece "Años de experiencia" ni "Viajeros felices", 0 nodos de la franja). Las secciones se solapan 56px por diseño (911→967) para alojar la tarjeta; con la tarjeta oculta, **32 muestras de `elementFromPoint` en 4 posiciones horizontales a lo ancho de la banda dan 0 elementos con texto propio**, todos DIV con fondo transparente: ni hueco visible ni texto pisado. Medido por Anyerson con scroll instantáneo y doble disparo.
- 3d. Paginación del carrusel: **PASA** — verificado destacando temporalmente un segundo tour (con 1 solo destacado el componente rinde tarjeta suelta, no carrusel: no había nada que medir). A **1440**: `maxScroll` 0, los 2 puntos miden **0x0** y hay **0 enfocables**. A **360**: `maxScroll` 248, los 2 puntos miden **24x24** y ambos enfocables. Dato restaurado (`is_featured=0` en tour id=2), confirmado por SQL. Medido por Anyerson.

## 4. Gate de regresión SEO (BLOQUEANTE) — **PASA COMPLETO**
- 4a. Las 3 páginas indexables (sin noindex): **PASA** — `/es/`, `/es/nosotros`, `/es/contacto` con 0 ocurrencias de `noindex` (verificado por Anyerson, curl)
- 4b. `/es/_styleguide` sigue noindex: **PASA** — `<meta name="robots" content="noindex, nofollow">` (verificado por Anyerson)
- 4c. Canonical autorreferencial con prefijo: **PASA** — `/es` → `.../es`, `/es/nosotros` → `.../es/nosotros`, `/es/contacto` → `.../es/contacto` (verificado por Anyerson)
- 4d. `hreflang` con `x-default`: **PASA** — las 3 páginas traen `hreflang="es"` y `hreflang="x-default"`, ambos autorreferenciales (verificado por Anyerson)
- 4e. Sitemap coincide con páginas reales: **PASA** — los 3 `<loc>` son exactamente `/es`, `/es/nosotros`, `/es/contacto`; sin la guía de estilos ni el panel (verificado por Anyerson)

## Veredicto
**Punto 1 (recorrido del dato del CMS): PASA.** El defecto bloqueante original (CMS sin
formulario para contacto/identidad legal/redes/cifras) está resuelto de punta a punta: el
panel tiene el formulario completo, el guardado con el botón real llega al front sin
intervención manual de caché, las cifras respetan la regla de "nunca 0/placeholder" (la
franja desaparece en vez de mostrar un dato inventado), y el badge RNAVT/MINCETUR aparece y
desaparece según haya o no dato real. Sin regresiones dentro de este punto.

**Punto 2 (Contacto de punta a punta): PASA con un hallazgo de severidad media.** El envío
real (adelantado del lote 3) funciona de punta a punta: se guarda en `contact_messages` con
los datos exactos del visitante, dispara el correo (a `storage/logs/laravel.log` porque
`MAIL_MAILER=log` en local, sin fallar el guardado si el envío falla), aparece y es
gestionable en el panel (`/admin/contact-messages`, clic real, cambio de estado con clic real
en "Guardar", persistido en BD), la validación de servidor bloquea el correo inválido sin
persistir nada, los errores salen por campo (no solo cartel genérico), y la regresión R2 del
CRO (select de Asunto que perdía la opción elegida) sigue arreglada. El hallazgo — mensajes de
error de reglas nativas sin traducir (`"validation.email"` literal en pantalla) por falta de
`lang/es/validation.php` — no invalida ninguno de los 4 subpuntos pedidos, pero es un defecto
real de cara al visitante. Se devuelve a `backend-laravel` (no es CSS/Blade, es lang file /
contrato de mensajes de validación).

**Veredicto general del lote: APTO.** Los cuatro puntos en PASA, incluidos los dos bloqueantes (Punto 1, recorrido del dato del CMS; Punto 4, gate de regresión SEO). Queda **un hallazgo de severidad media** devuelto a `backend-laravel`: los mensajes de validación de reglas nativas se muestran como la clave cruda (`"validation.email"`) por falta de `lang/es/validation.php`. No bloquea el veredicto pero debe cerrarse antes de mostrar el sitio a la clienta.


## No verificado
- Punto 3 completo (regresión visual/funcional en 360/768/1024/1280/1440, consola/red,
  tarjeta flotante de estadísticas, paginación del carrusel) — fuera de este alcance.
- Consola/red del navegador durante el flujo de Contacto (2a-2c): no se inspeccionaron
  mensajes de consola ni requests fallidas con esta herramienta (playwright-core vía CDP sin
  los listeners de red del MCP); no se observó nada anómalo a simple vista pero no se
  verificó explícitamente.
- El envío real de correo (SMTP): no aplica en local (`MAIL_MAILER=log`, declarado no-defecto
  por el orquestador); no se abrió `storage/logs/laravel.log` para confirmar que el log se
  escribió, solo se confirmó que el guardado en BD no depende de que el correo funcione (try/
  catch en el controller).
- Cualquier otra sección del CMS fuera de `/admin/configuracion` y `/admin/contact-messages`
  (Equipo, Destinos, Experiencias) — no estaba en el alcance recortado.

## Confirmación final de BD (SQL, post-limpieza)
```
settings: 1 fila → exchange_rate_pen_usd = 3.75
users: 1
team_members: 0
destinations con cover_image_path: 0
contact_messages: 0 (limpiado tras 2a/2b; 2c no llegó a persistir por validación fallida)
```
Confirmado también en el front (curl, tras `cache:clear`): `/es`, `/es/nosotros`,
`/es/contacto` no contienen ningún dato de prueba de esta pasada.

## Hallazgos propios del orquestador (fuera del alcance de QA)

1. **`tailwind.config.js` nunca se eliminó.** El reporte de la ronda 1 y el mensaje del commit
   `3bb5831` afirman que se borró; sigue en disco y trackeado por git (fecha 02/09 21:59, lo más
   probable es que un `git checkout` de recuperación del bug de EPERM lo restaurara). **No hay
   regresión funcional:** no existe ninguna directiva `@config` activa en `resources/css/` (los
   dos aciertos del grep son comentarios que explican por qué no se usó), así que Tailwind 4 no
   lo lee y la marca sale de `tokens.css` vía `@theme`. Es **código muerto que engaña al
   próximo que lo abra**, y el mensaje del commit está mal. Pendiente: borrarlo de verdad y
   dejar el registro corregido.

2. **Las fuentes de marca se sirven desde Google Fonts** (`fonts.googleapis.com` +
   `fonts.gstatic.com`, 4 peticiones externas por página, todas 200 en local). En producción, la
   política de seguridad de contenido tiene que permitir esos dos orígenes o **Fraunces, Figtree
   y Caveat fallan en silencio** y el sitio cae a las fuentes de respaldo sin ningún error
   visible. En otro proyecto de la cartera una CSP bloqueó la analítica exactamente así.
